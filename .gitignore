# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
*~
.vscode/
.idea/

# Local-only environment & notes
.env
.env.local
*.local
notes/
TODO.local.md

# Claude Code local config — but skills are shared, so cloud sessions that can
# only see this repo still pick up the build standard.
.claude/*
!.claude/skills/

# Design exploration & internal docs (not part of the live site)
design-philosophy.md
kinetic-trust-design-framework.pdf
seo-audit-*.md

# Preview / working iteration files
preview.html
style-preview.css

# Node tooling (if you install minifiers locally)
node_modules/
package-lock.json
yarn.lock
.npm/

# Build / cache / logs
*.log
__pycache__/
*.pyc
.cache/
dist/
build/
